import React from "react";
import ModeButton from "./ModeButton";

function Header() {
    return (
        <div>
            <h1>
                Spacestagram
            </h1>
            <ModeButton/>
        </div>
    );
}

export default Header;
